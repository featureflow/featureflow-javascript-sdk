dependencies:
  pre:
  - npm install -g karma-cli
  cache_directories:
  - ~/nvm
  override:
  - sudo pip install awscli
deployment:
  production:
    branch: master
    commands:
    - chmod +x ./deploy_prod_s3.sh
    - ./deploy_prod_s3.sh

